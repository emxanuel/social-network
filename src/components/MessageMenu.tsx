import styles from '../css/messageMenu.module.css'

const MessageMenu = () => {
    return (
        <div className={`${styles.container} ${styles.notShow} ${styles.hidden}`}>

        </div>
    )
}

export default MessageMenu